import { ArrowLeft,LockKeyhole } from 'lucide-react'
import { FormEvent,useState } from 'react'
import { Link,useLocation,useNavigate } from 'react-router-dom'
import { useAuth } from '@/auth/AuthProvider'
import { Button } from '@/components/ui'
export function LoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const{signIn,profile}=useAuth();const navigate=useNavigate();const location=useLocation();async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const message=await signIn(email,password);setBusy(false);if(message)setError(message);else navigate((location.state as {from?:{pathname:string}})?.from?.pathname||`/${profile?.role??'student'}`)}return <div className="grid min-h-screen bg-slate-50 lg:grid-cols-2"><div className="hidden bg-inha-950 p-12 text-white lg:flex lg:flex-col lg:justify-between"><Link to="/" className="font-black">INHA App Arch Lab</Link><div><p className="text-sm font-black tracking-widest text-blue-300">DESIGN · BUILD · PRESENT</p><h1 className="mt-5 text-5xl font-black leading-tight">프로젝트의 모든 진행을<br/>한곳에서 관리하세요.</h1></div><p className="text-sm text-slate-400">Application Architecture · 2026 Summer</p></div><div className="grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-soft sm:p-10"><span className="grid h-12 w-12 place-items-center rounded-xl bg-blue-50 text-inha-700"><LockKeyhole/></span><h2 className="mt-7 text-3xl font-black">로그인</h2><p className="mt-2 text-sm text-slate-500">관리자가 등록한 이메일과 비밀번호를 입력하세요.</p><label className="mt-8 block text-sm font-bold">이메일<input className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-blue-500" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="mt-5 block text-sm font-bold">비밀번호<input className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-blue-500" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button className="mt-7 w-full" disabled={busy}>{busy?'로그인 중…':'로그인'}</Button><Link to="/" className="mt-6 flex items-center justify-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft className="h-4 w-4"/>소개 페이지로</Link></form></div></div>}
